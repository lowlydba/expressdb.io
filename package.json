{
  "name": "expressdb",
  "version": "1.0.0",
  "description": "expressdb.io",
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "install": "npm install --no-audit --no-fund --loglevel=error --ignore-scripts"
  },
  "devDependencies": {
    "@vuepress/plugin-google-analytics": "^2.0.0-rc.0",
    "vuepress": "^2.0.0-rc.0",
    "@vuepress/shared-utils": "^1.0.0-rc.1",
    "@vuepress/theme-default": "^2.0.0-rc.0"
  },
  "dependencies": {}
}
